services:
  nightwatch-agent:
    image: laravelphp/nightwatch-agent:v1
    environment:
      - NIGHTWATCH_TOKEN=${NIGHTWATCH_TOKEN}
    healthcheck:
      test: ["CMD", "php", "/usr/local/bin/nightwatch-status"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s